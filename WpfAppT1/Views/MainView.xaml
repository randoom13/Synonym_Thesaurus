<Window x:Class="WpfAppT1.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppT1.Views"
        Title="Thesaurus" 
        Height="300" 
        Width="300">
    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
            <Setter Property="HorizontalAlignment"
                    Value="Stretch"/>
            <Setter Property="Height"
                    Value="20"/>
            <Setter Property="Minimum"
                    Value="0"/>
            <Setter Property="IsIndeterminate"
                    Value="True"/>
            <Setter Property="Maximum"
                    Value="100"/>
        </Style>
    </Window.Resources>
    <TabControl>
        <TabItem Header="Add synonyms">
            <ContentControl Name="NewSynonyms"/>
        </TabItem>
        <TabItem Header="Search synonyms">
            <ContentControl Name="SearchSynonyms"/>
        </TabItem>
        <TabItem Header="All words">
            <ContentControl Name="AllWords"/>
        </TabItem>
    </TabControl>
</Window>
